var React = require('react'),
    zeroclipboard = require('zeroclipboard');

module.exports = React.createClass({
    render(): any {
        return <div>
            hello world!
        </div>;
    }
});